syntax = "proto3";
package aoa.schemas;

message AcousticFrame {
  string stream_id = 1;
  uint64 timestamp_utc_ms = 2;
  bytes payload_pcm = 3;   // raw or compressed frames
  uint32 sample_rate_hz = 4;
  uint32 channels = 5;
}